using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Effects {
    public enum Effect { Attack, Defence, MovementSpeed,  IncreaseHealth };
    public Effect effect = Effect.Attack;
}
